# HTML: always revalidate

<FilesMatch "\.html?$">

	Header set Cache-Control "no-cache"

</FilesMatch>

# Hashed CSS/JS: cache aggressively

<FilesMatch "\.[a-f0-9]{8}\.(css|js)$">

	Header set Cache-Control "public, max-age=31536000, immutable"

</FilesMatch>